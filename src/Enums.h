#ifndef ENUMS_h
#define ENUMS_h

enum class E_SignalLED { GESTURE, OVERRIDE, LOWBAT, BLUETOOTH };

enum class MotorState { STOP, FORWARD, REVERSE };

enum class ControllerState { IDLE, FORWARD, REVERSE, LEFT, RIGHT, STOP };

#endif